# Io Pet - Desktop Companion
# Core dependencies
PyQt5>=5.15.0
requests>=2.28.0

# 注意：窗口追踪已改用 LocalAgent API，不再需要 pywin32

# Voice (optional but recommended)
sounddevice>=0.4.6
soundfile>=0.12.0
numpy>=1.24.0
faster-whisper>=0.10.0
edge-tts>=6.1.0
